// @ts-check
import { FUNCTION, UNDEFINED, T_UNDEFINED } from "reshow-constant";
/**
 * @param {Function|any} maybeFunc
 * @param {Array} args
 * @param {any} scope
 * @param {any} def
 * @returns {any}
 */

var callFunc = function callFunc(maybeFunc, args, scope, def) {
  if (args === void 0) {
    args = [];
  }

  if (scope === void 0) {
    scope = T_UNDEFINED;
  }

  if (def === void 0) {
    def = T_UNDEFINED;
  }

  return FUNCTION === typeof maybeFunc ? maybeFunc.apply(scope, args) : UNDEFINED !== typeof def ? def : maybeFunc;
};

export default callFunc;