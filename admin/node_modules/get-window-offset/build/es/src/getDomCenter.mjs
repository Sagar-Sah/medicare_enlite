import getDomPositionInfo from "./getDomPositionInfo.mjs";
var calDomCenter = function calDomCenter(left, top, width, height) {
  var xy = [left + Math.floor(width / 2), top + Math.floor(height / 2)];
  return xy;
};
var getDomCenter = function getDomCenter(dom) {
  var _getDomPositionInfo;
  var {
    left,
    top,
    width,
    height
  } = ((_getDomPositionInfo = getDomPositionInfo(dom)) === null || _getDomPositionInfo === void 0 ? void 0 : _getDomPositionInfo.domInfo) || {};
  var domCenter = calDomCenter(left, top, width, height);
  return domCenter;
};
export default getDomCenter;