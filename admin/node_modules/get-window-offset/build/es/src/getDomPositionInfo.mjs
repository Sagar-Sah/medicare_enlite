import getOffset from "getoffset";
import isFixed from "./isFixed.mjs";
import isSetOverflow from "./isSetOverflow.mjs";
var getDomPositionInfo = function getDomPositionInfo(dom) {
  var fixedNode = isFixed(dom);
  var scrollNode = isSetOverflow(dom);
  var domInfo = getOffset(dom, fixedNode);
  domInfo.scrollNode = scrollNode;
  domInfo.fixedNode = fixedNode;
  return {
    domInfo,
    fixedNode,
    scrollNode
  };
};
export default getDomPositionInfo;