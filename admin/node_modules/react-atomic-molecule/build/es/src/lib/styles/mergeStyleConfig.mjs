import _objectSpread from "reshow-runtime/es/helpers/objectSpread2";
import { KEYS } from "reshow-constant";
import mergeDefaultValue from "../../mergeDefaultValue.mjs";
/**
 * @param {object} styles
 * @param {object} defaultStyles
 * @param {object} injectStyles
 */

var mergeStyleConfig = function mergeStyleConfig(styles, defaultStyles, injectStyles) {
  if (defaultStyles) {
    mergeDefaultValue(styles, defaultStyles);
  }

  if (injectStyles) {
    KEYS(styles).forEach(function (key) {
      if (injectStyles[key]) {
        injectStyles[key][0] = _objectSpread(_objectSpread({}, injectStyles[key][0]), styles[key]);
      }
    });
  }
};

export default mergeStyleConfig;