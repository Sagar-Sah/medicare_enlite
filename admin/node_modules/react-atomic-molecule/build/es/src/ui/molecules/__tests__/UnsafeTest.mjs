var _Unsafe, _Unsafe2;

import React from "react";
import { expect } from "chai";
import { render } from "reshow-unit";
import Unsafe from "../Unsafe.mjs";
import { jsx as _jsx } from "react/jsx-runtime";
describe("Test Unsafe", function () {
  it("basic test", function () {
    var wrapper = render(_Unsafe || (_Unsafe = /*#__PURE__*/_jsx(Unsafe, {})));
    expect(wrapper.html()).to.have.string("us-html");
  });
  it("test string", function () {
    var wrapper = render(_Unsafe2 || (_Unsafe2 = /*#__PURE__*/_jsx(Unsafe, {
      children: "<br />"
    })));
    expect(wrapper.html()).to.equal("<div class=\"us-html ui\"><br></div>");
  });
  it("test function", function () {
    var wrapper = render( /*#__PURE__*/_jsx(Unsafe, {
      children: function children() {
        return "<br />";
      }
    }));
    expect(wrapper.html()).to.equal("<div class=\"us-html ui\"><br></div>");
  });
});