import { useEffect } from "react";
import { T_FALSE, T_TRUE } from "reshow-constant";
import useEnable from "./useEnable.mjs";

var useLoaded = function useLoaded(onChange) {
  var [isLoad, setIsLoad] = useEnable(T_TRUE, T_FALSE, onChange);
  useEffect(function () {
    setIsLoad();
  }, []);
  return isLoad;
};

export default useLoaded;