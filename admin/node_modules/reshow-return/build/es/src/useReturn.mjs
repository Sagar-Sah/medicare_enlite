import { useConnect } from "reshow-flux";
import connectOptions from "./connectOptions.mjs";

var useReturn = function useReturn(initStates, store, _temp) {
  var {
    pathStates,
    immutable = true
  } = _temp === void 0 ? {} : _temp;
  var state = useConnect(connectOptions)({
    store,
    initStates,
    pathStates,
    immutable
  });
  return state;
};

export default useReturn;