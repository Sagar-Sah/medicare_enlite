import { getReturn, ClientReturn } from "reshow-return";
import urlStore from "../../stores/urlStore.mjs";
var UrlReturn = getReturn({
  displayName: "UrlReturn",
  options: {
    storeLocator: function storeLocator() {
      return urlStore;
    }
  }
});
export default ClientReturn(UrlReturn);