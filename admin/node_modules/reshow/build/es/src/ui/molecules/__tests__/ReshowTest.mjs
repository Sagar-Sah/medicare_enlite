import _asyncToGenerator from "reshow-runtime/es/helpers/asyncToGenerator";

var _div;

import React from "react";
import Reshow from "../Reshow.mjs";
import { globalStore } from "../../../stores/globalStore.mjs";
import dispatch from "../../../dispatch.mjs";
import { expect } from "chai";
import { waitFor, render, cleanIt } from "reshow-unit";
import { jsx as _jsx } from "react/jsx-runtime";
describe("Test Reshow Component", function () {
  afterEach(function () {
    dispatch("config/reset");
    globalStore.path = null;
    cleanIt();
  });
  it("Reshow simple test", /*#__PURE__*/_asyncToGenerator(function* () {
    var wrapper = render( /*#__PURE__*/_jsx(Reshow, {
      themes: {
        hello: _div || (_div = /*#__PURE__*/_jsx("div", {
          children: "Hello World!"
        }))
      },
      themePath: "hello"
    }));
    yield waitFor(function () {
      return expect(wrapper.html()).to.equal("<div>Hello World!</div>");
    });
  }));
});