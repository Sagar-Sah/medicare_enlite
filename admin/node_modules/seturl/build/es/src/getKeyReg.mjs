import getSafeReg, { cacheReg } from "get-safe-reg";

var getRegString = function getRegString(name) {
  return "(([#?&])" + getSafeReg(name) + "=)([^&#]*)";
};

var cache = cacheReg({})(getRegString);
var multiMatchCache = cacheReg({})(getRegString, "g");

var getMultiMatchReg = function getMultiMatchReg(name) {
  return multiMatchCache(name);
};

export default (function (name) {
  return cache(name);
});
export { getMultiMatchReg };