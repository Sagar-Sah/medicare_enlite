import { FUNCTION } from "reshow-constant";

var getDefaultValue = function getDefaultValue(v, cur) {
  return FUNCTION === typeof v ? v(cur) : v !== null && v !== void 0 ? v : cur;
};

export default getDefaultValue;