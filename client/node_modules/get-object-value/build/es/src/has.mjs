import { HAS } from "reshow-constant";
import callfunc from "call-func";

var HasMap = function HasMap(o, k) {
  var _ref;

  return (_ref = o && o.size && callfunc(o.has, [k], o)) !== null && _ref !== void 0 ? _ref : HAS(o, k);
};

export default HasMap;