import { expect } from "chai";
import { jsdom } from "reshow-unit-dom";
import needCSS from "../index.mjs";
describe("Test need-css", function () {
  beforeEach(function () {
    jsdom(null, {
      runScripts: "dangerously",
      resources: "usable"
    });
  });
  it("basic testt", function (done) {
    needCSS(["prism"]);
    setTimeout(function () {
      expect(document.getElementsByTagName("link").length).to.equal(1);
      done();
    }, 500);
  });
});