import { expect } from "chai";
import nest from "../nest.mjs";
describe("Test nest", function () {
  it("basic test", function () {
    var a = {
      "a.b.c": "foo"
    };
    var result = nest(a);
    expect(result["a"]["b"]["c"]).to.equal("foo");
  });
});