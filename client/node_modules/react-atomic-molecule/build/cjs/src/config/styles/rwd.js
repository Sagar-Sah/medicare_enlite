"use strict";

exports.__esModule = true;
exports.pageWidth = exports.min = exports.max = void 0;

var _reshowConstant = require("reshow-constant");

//@ts-check

/**
 * https://purecss.io/grids/#pure-responsive-grids
 * https://www.w3schools.com/tags/ref_pxtoemconversion.asp
 *
 * if default pixel size eq 16px
 *
 * 35.5.em -> 568px
 * 48em -> 768px
 * 64em -> 1024px
 * 80em -> 1280px
 * 120em -> 1920px
 */
var pageWidth = {
  sm: "35.5em",
  md: "48em",
  lg: "64em",
  xl: "80em",
  xxl: "120em"
};
/**
 * @typedef PageWidth
 * @type {any}
 */

/**
 * @param {string} type
 * @returns {PageWidth}
 */

exports.pageWidth = pageWidth;

var getConfig = function getConfig(type) {
  var next = (0, _reshowConstant.NEW_OBJ)();
  (0, _reshowConstant.KEYS)(pageWidth).forEach(function (key) {
    next[key] = "@media screen and (" + type + "-width: " + pageWidth[key] + ")";
  });
  return next;
};

var min = getConfig("min");
exports.min = min;
var max = getConfig("max");
exports.max = max;