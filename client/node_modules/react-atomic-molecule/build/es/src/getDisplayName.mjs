import get from "get-object-value";
var DISPLAY_NAME = "displayName";
var TYPE = "type";

var getDisplayName = function getDisplayName(el) {
  return get(el, [DISPLAY_NAME], get(el, [TYPE, DISPLAY_NAME]));
};

export default getDisplayName;