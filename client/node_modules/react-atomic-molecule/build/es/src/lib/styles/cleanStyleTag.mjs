import { KEYS } from "reshow-constant";
import { remove } from "create-el";
import query from "css-query-selector";
import getStyleTagId from "./getStyleTagId.mjs";

var cleanStyleTag = function cleanStyleTag(StyleMap) {
  KEYS(StyleMap || []).forEach(function (key) {
    var id = getStyleTagId(StyleMap[key].styleId);
    var styleDom = query.one("#" + id);
    remove(styleDom);
  });
};

export default cleanStyleTag;