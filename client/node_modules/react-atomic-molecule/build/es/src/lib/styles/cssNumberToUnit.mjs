import { isUnitlessNumber } from "./cssUnitLessNumber.mjs";
import { IS_ARRAY, NUMBER } from "reshow-constant";

var appendPx = function appendPx(v) {
  return v && NUMBER === typeof v ? v + "px" : v;
};

var cssNumberToUnit = function cssNumberToUnit(key, value) {
  if (isUnitlessNumber[key]) {
    return value;
  }

  return IS_ARRAY(value) ? value.map(function (v) {
    return appendPx(v);
  }) : appendPx(value);
};

export default cssNumberToUnit;