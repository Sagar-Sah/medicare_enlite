import _objectSpread from "reshow-runtime/es/helpers/objectSpread2";
import { KEYS, IS_ARRAY } from "reshow-constant";

var isObject = function isObject(val) {
  return val != null && typeof val === "object" && IS_ARRAY(val) === false;
};

var mergeDefaultValue = function mergeDefaultValue(obj, defaultValue) {
  obj = obj || {};
  KEYS(defaultValue).forEach(function (key) {
    obj[key] = isObject(defaultValue[key]) ? _objectSpread(_objectSpread({}, defaultValue[key]), obj[key]) : null != obj[key] ? obj[key] : defaultValue[key];
  });
  return obj;
};

export default mergeDefaultValue;