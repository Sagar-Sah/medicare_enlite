import { useReducer } from "react";
import callfunc from "call-func";
/**
 * React useReducer
 * https://reactjs.org/docs/hooks-reference.html#usereducer
 */

var useEnable = function useEnable(setTo, initVal, onChange) {
  var reducer = useReducer(function () {
    return callfunc(onChange, [setTo]) || setTo;
  }, initVal, function (initVal) {
    var _callfunc;

    return (_callfunc = callfunc(onChange, [initVal])) !== null && _callfunc !== void 0 ? _callfunc : initVal;
  });
  return reducer;
};

export default useEnable;