import { ImmutableStore } from "reshow-flux";
var [store, realTimeDispatch] = ImmutableStore(function (state, action) {
  switch (action.type) {
    case "realTime":
      return action.params || [];

    default:
      return [];
  }
});
export default store;
export { realTimeDispatch };