import { expect } from "chai";
import { objectToArray } from "../index.mjs";
describe("Test objectToArray", function () {
  it("test objectToArray", function () {
    var o = {
      foo1: "bar1",
      foo2: "bar2"
    };
    expect(objectToArray(o)).to.deep.equal(["bar1", "bar2"]);
  });
});